# AcadBus

rnfe

app-
code .
npm rum android

banco-
node index.js

nota para o Michell
const PORT = 9221;

const db = mysql.createConnection({
    host: 'localhost',
    user: 'root',
    password: '*****', - Voce sabe
    database: 'acadbus',
    port: 3306
});
